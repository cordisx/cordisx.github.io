{
  "name": "cordisx-ai-plugin-demo",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Isolated source fixture for the real CordisX AI-first capture",
  "type": "module",
  "scripts": {
    "check": "npm run typecheck && npm run dev:dry-run",
    "dev:dry-run": "cordisx dev ./src/celebration.tsx --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "4.0.1",
    "cordisx": "0.1.0-beta.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22.19"
  }
}
