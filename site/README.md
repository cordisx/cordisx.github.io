# Homepage Site

The static site is maintained at the repository root: `index.html`, shared
styles and scripts, `marketplace/`, and `assets/`. This directory contains
only this navigation note.

See the [repository README](../README.md) for the project entry and the
[maintenance documentation](../.agents/docs/README.md) for design, Marketplace,
and capture workflows. Available checks are defined in
[`package.json`](../package.json).
